// import { Header } from "features/header/components/Header";
// import { PosterDisplayAll } from "features/viewAllPostersSection/components/PosterDisplayAll";
// import { useViewAll } from "features/viewAllPostersSection/hooks";
// import { useState } from "react";
// import { useParams } from "react-router-dom";

export const ShowAll = () => {

  // const [pageNumber, setPageNumber] = useState(1);
  // const {section, type} = useParams();

    return (<div className="showAll">
      {/* <Header></Header>
      <h2 className="sectionName">{section}</h2>
      <PosterDisplayAll />
      <button className="moreMovies" onClick={() => {
        setPageNumber(prev => prev + 1);
      }}>More</button> */}
    </div>);
}